using System;
using System.IO;
using System.Linq;
using HerdtraxImport;

namespace TestHarness
{
    public class CalvingFileReaderTests
    {
        public void Test()
        {
/*

            var sr = new StreamReader(TestingConstants.TestCSVFileName);

            // First row must be the headings
            string headings = sr.ReadLine();
            if (headings == null)
                throw new ApplicationException(string.Format("Empty file: {0}", TestingConstants.TestCSVFileName));

            var _calvingFileReader = new CalvingFileReader();

            var coldefs = _calvingFileReader.ProcessHeadings(headings);
*/



            

        }

    }
}